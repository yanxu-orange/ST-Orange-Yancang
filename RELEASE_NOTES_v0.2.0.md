# 砚藏 v0.2.0

画师串库的首个可手动使用版本。下载安装包：`Yancang-0.2.0-release.apk`。

## 本次更新

★ 新增画师串库

- 支持本地图片和单张 PNG／JPG／JPEG 图片直链 URL 导入；导入后自动解析图片元数据中可识别的画师串与生成参数。
- 可快捷复制提示词，填写备注与来源链接，并点击来源链接跳转。
- 支持独立的自定义 Tag、标签筛选、常规编辑和另存为独立资源；也可导出原图与结构化资料。
- 多张本地图片导入时，损坏或不可读的图片会单独提示，不再阻断其余正常图片。

如需进一步解析，可在设置中指定解析网址，并从画师串详情跳转，在系统浏览器中打开；砚藏不会自动上传图片。

## 安装与更新

建议先在“设置 → 数据管理”导出完整备份，再覆盖安装。请从本 Release 下载 APK；旧版本和下载记录会保留。

智绘姬设置 JSON、Vibe 与 SillyTavern 实时画师串联动尚未接入本版本。画师串页面的进一步美化会在后续版本处理。
